
---

## Упражнения

Упражнения 29.1, 29.4 и 29.6 — практические, их делай на стенде.
29.2, 29.3, 29.5 и 29.7 решаются на бумаге.

**29.1.** *(практика, до первого запуска)* Перед тем как впервые
сказать `sc start Hello`, запиши на бумаге свои предсказания:
а) с каких цифр начнутся оба адреса и почему; б) какой будет PID;
в) какой будет IRQL. Потом запусти и сравни. Где ошибся — объясни
себе, почему.

**29.2.** а) Сколько байт занимает структура `UNICODE_STRING` в
64-битной Windows и на каком смещении лежит поле `Buffer`? Вспомни
правила выравнивания из приложения П1. б) Чему равны `Length` для
строки `L"Hello"` и для пути
`\REGISTRY\MACHINE\SYSTEM\ControlSet001\Services\Hello`? в) Почему
для строк ядра хранят длину, а не полагаются на ноль в конце?

**29.3.** В реестре виртуальной машины для компонента `IHVDRIVER`
записана маска фильтра `0x5`. Драйвер вызывает `DbgPrintEx` четыре
раза — с уровнями `ERROR`, `WARNING`, `TRACE` и `INFO`. Какие
сообщения увидит DebugView? Посчитай по битам.

**29.4.** *(практика)* Сделай копию драйвера, в которой `DriverEntry`
печатает свой «привет», а возвращает не `STATUS_SUCCESS`, а
`STATUS_UNSUCCESSFUL` («неудача»). До запуска предположи:
а) что покажет `sc start`; б) появится ли «привет» в DebugView;
в) будет ли вызвана `HelloUnload`; г) что ответит потом `sc stop`.
Проверь.

**29.5.** Вот начало структуры `DRIVER_OBJECT` из заголовка
(типы: `CSHORT` — 2 байта, `ULONG` — 4 байта, `UNICODE_STRING` —
смотри упражнение 29.2, все `P...` и `PVOID` — указатели):

```c
typedef struct _DRIVER_OBJECT {
    CSHORT Type;
    CSHORT Size;
    PDEVICE_OBJECT DeviceObject;
    ULONG Flags;
    PVOID DriverStart;
    ULONG DriverSize;
    PVOID DriverSection;
    PDRIVER_EXTENSION DriverExtension;
    UNICODE_STRING DriverName;
    PUNICODE_STRING HardwareDatabase;
    PFAST_IO_DISPATCH FastIoDispatch;
    PDRIVER_INITIALIZE DriverInit;
    PDRIVER_STARTIO DriverStartIo;
    PDRIVER_UNLOAD DriverUnload;
    PDRIVER_DISPATCH MajorFunction[28];
} DRIVER_OBJECT;
```

Посчитай смещение каждого поля до `DriverUnload` включительно.
Совпадает ли ответ с `[rbx+0x68]` из дизассемблера в разделе 29.5?

**29.6.** *(практика)* Разбери три ситуации: что случилось и как
исправить?
а) `sc create Hello type=kernel binPath=C:\drivers\Hello.sys` вывела
длинную справку вместо «SUCCESS».
б) `sc start Hello` ответила ошибкой 577, хотя вчера всё работало.
Ты вчера вернул снимок машины «чистая».
в) Ты поправил код, собрал, скопировал `Hello.sys` в `C:\drivers\`,
а Проводник пишет, что файл занят и не может быть заменён.

**29.7.** В чужом драйвере ты нашёл функцию. Параметр `req` — адрес,
который прислала программа из кольца 3, сам адрес уже проверен
(`ProbeForRead` вызван). Найди **две** оставшиеся проблемы и перепиши
функцию правильно:

```c
ULONG SumValues(REQUEST *req)
{
    ULONG sum = 0;
    if (req->Count > 16)
        return 0;
    for (ULONG i = 0; i < req->Count; i++)
        sum += req->Values[i];
    return sum;
}
```

> Ответы — в [ANSWERS.md](../ANSWERS.md).

**Тренажёр.** Закрепи главу в
[тренажёре](https://claude.ai/artifact/2UULEnh9wsTTKAgk9So5Xf), группа
«гл. 29 · первый драйвер»: фильтр `DbgPrintEx` по битам, смещения
в `DRIVER_OBJECT`, команды `sc` и их ошибки, где и на каком IRQL
выполняется код драйвера.

---

## Что дальше

Твой код побывал в кольце 0. Но смотрели мы на него пока через
замочную скважину: он печатает строчки, а мы их читаем. Когда драйвер
упал, мы увидели только синий экран с названием ошибки.

В [главе 30](30-otladka-yadra.md) мы откроем дверь целиком. Отладчик
ядра **WinDbg** из основной машины подключится к ядру виртуальной —
и ты сможешь остановить **всю Windows** на своей строчке, пройти
`DriverEntry` по шагам, открыть дамп после синего экрана и найти
строку, на которой упал драйвер. Там же мы наконец сверим
с живой системой обещания части X: настоящие `CR0` и `CR3`, записи
таблиц страниц (`!pte`) и таблицу прерываний (`!idt`).

---

| | |
|---|---|
| **Предыдущая** | [28. Системные вызовы](28-sistemnye-vyzovy.md) |
| **Оглавление** | [README](../README.md) |
| **Словарь** | [GLOSSARY](../GLOSSARY.md) |
| **Следующая** | [30. Отладка ядра](30-otladka-yadra.md) |
